GOOGLE_CLOUD_PROJECT: "trademark-prediction-system"
GOOGLE_CLOUD_LOCATION: "europe-west1"
GOOGLE_GENAI_USE_VERTEXAI: "true"
TEST_MODE: "false"

# Use this format in deployment to avoid environment variable concatenation issues
# Each variable should be set individually with proper quoting